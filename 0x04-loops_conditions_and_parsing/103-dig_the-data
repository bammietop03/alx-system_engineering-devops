#!/usr/bin/env bash

# Use awk to group visitors by IP and HTTP status code, count occurrences, and display in the specified format
awk '{count[$1" "$9]++} END {for (key in count) {print count[key], key}}' apache-access.log | sort -rn
